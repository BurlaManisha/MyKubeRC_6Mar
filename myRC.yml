apiVersion: v1
kind: ReplicationController
metadata:
  name: myonlinerc-demo
spec:
  replicas: 3
  selector:
    app: myonstore
    version: 2.6.1
  template:
    metadata:
      labels:
        app: myonstore
        version: 2.6.1
    spec:
      containers:
        - name: myonlineapp-container
          image: maludockerhub/myimg23feb23:1
          ports:
            - containerPort: 8080